import type { WeatherData, WeatherProps } from './types';

import { useWeatherData } from './useWeatherData';
import Weather from './Weather';

export { useWeatherData, type WeatherData, type WeatherProps };
export default Weather;
